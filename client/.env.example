# Copy to client/.env for local development, and set the same variable in
# the Netlify build settings for production.

# Backend API URL, without a trailing slash, e.g. http://localhost:8000
VITE_BASE_URL=
